-- This script was generated by a beta version of the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.company
(
    code bigserial NOT NULL,
    name text,
    email text,
    password text,
    tax text,
    PRIMARY KEY (code)
);

CREATE TABLE IF NOT EXISTS public.client
(
    code bigserial NOT NULL,
    name text,
    email text,
    password text,
    PRIMARY KEY (code)
);

CREATE TABLE IF NOT EXISTS public.loan
(
    code bigserial NOT NULL,
    total text,
    installments bigint,
    state text,
    code_client bigint,
    code_company bigint,
    PRIMARY KEY (code)
);

ALTER TABLE public.loan
    ADD FOREIGN KEY (code_client)
    REFERENCES public.client (code)
    NOT VALID;


ALTER TABLE public.loan
    ADD FOREIGN KEY (code_company)
    REFERENCES public.company (code)
    NOT VALID;

END;
